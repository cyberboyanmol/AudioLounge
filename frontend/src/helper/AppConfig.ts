const AppConfig = {
  DEFAULT_SERVER_ERROR:
    "A temporary issue prevented us from processing your request . Please try again later.",
  BASE_URL: `${process.env.NEXT_PUBLIC_BACKEND_API_URL}/api/v1`,
};

export default AppConfig;
